# Supabase (tables only; no Supabase Auth required)
NEXT_PUBLIC_SUPABASE_URL=https://pcvnurlxthouguipgnna.supabase.co
# The key below appears to be a publishable key (starts with sb_publishable), so it is safe to use as the anon key.
# We do not use the SERVICE_ROLE_KEY here to avoid exposing admin privileges.
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xfnZSkr9YLjzYc7QaIRukA_hwMcGVZD
SUPABASE_SERVICE_ROLE_KEY=sb_publishable_xfnZSkr9YLjzYc7QaIRukA_hwMcGVZD

# App security
APP_SESSION_SECRET=change_this_to_a_long_random_string

# Optional: set initial admin (seed) credentials
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=admin123
